# YENTL-MOBILE-BUILD-0005

Timestamp: 2026-06-08T23:20:33-04:00
Lane: mobile-ui-build
Result: verified_done

## Scope

Consumed exact `ready_for_build` row `YENTL-MOBILE-BUILD-0005`: populated mobile-web learn-route handoff from detail footer links.

Allowed files touched:

- `components/session/claim-learn-more.tsx`
- `components/session/marker-learn-more.tsx`
- `components/session/learn-more.tsx`
- `tests/learn-more.test.tsx`

No API, legal/privacy copy, source-capture policy, native iOS/Android implementation, TV app implementation, annotation detail route change, broad learning redesign, dependency install, git staging, commit, push, or deploy was performed.

## What changed

- Added 44px-class guards and stable hooks to learn-route back controls.
- Added 44px-class guard and stable hook to the shared learn-route NotFound return control.
- Added 44px-class guards and stable hooks to claim related-claim links.
- Added 44px-class guards and stable hooks to marker further-reading links, occurrence links, and related-pattern links.
- Added focused `LearnMore` tests for claim learn, marker learn, and missing-item mobile tap-target guards.

## Pre-fix proof

`npx vitest run tests/learn-more.test.tsx -t "mobile learn-route tap targets"` failed before implementation because:

- `learn-back-btn` did not exist on claim or marker learn routes.
- `learn-notfound-back-btn` did not exist.
- Claim related links, marker reading links, marker occurrence links, and marker related-pattern links had no testable 44px mobile target guard.

## Verification

Passed:

- `npx vitest run tests/learn-more.test.tsx -t "mobile learn-route tap targets"`: 3 passed.
- `npx vitest run tests/learn-more.test.tsx`: 24 passed.
- `npm run build:automation`: passed, including Next TypeScript step and all 39 app routes.
- `npx tsc --noEmit`: passed after the build.

## Rendered 390px proof

Browser viewport: 390x844.

Claim learn route:

- URL: `http://localhost:3000/session/learn/claim/israel_010-claim-1?demo=validation&sample=israel_010`
- Populated content found: `Sources & context`
- NotFound copy absent.
- Horizontal overflow: false (`docWidth=390`, `viewportWidth=390`).
- Bad measured targets below 44px: 0.
- Measured visible targets:
  - `learn-back-btn`: 44px height
  - `claim-related-link`: 13 visible related-claim links, 44px height each

Marker learn route:

- URL: `http://localhost:3000/session/learn/marker/vagueness?demo=validation&sample=cable_008`
- Populated content found: `Vagueness / Hand-waving`
- NotFound copy absent.
- Horizontal overflow: false (`docWidth=390`, `viewportWidth=390`).
- Bad measured targets below 44px: 0.
- Measured visible targets:
  - `learn-back-btn`: 44px height
  - `learn-reading-link`: 2 visible reading links, 59px height each
  - `marker-occurrence-link`: 44px height
  - `marker-related-link`: 6 visible related-pattern links, 44px height each

## Current truth

`YENTL-MOBILE-BUILD-0005` is verified for mobile-web claim and marker learn routes only. This does not prove native iOS, native Android, TV, legal/privacy copy, source-capture behavior, or broader learning redesign readiness.
