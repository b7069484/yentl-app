# Yentl Mobile UI Build Evidence: YENTL-MOBILE-BUILD-0003

Timestamp: 2026-06-08T22:56:58-04:00
Loop: `mobile-ui-build`
Row: `YENTL-MOBILE-BUILD-0003`
Outcome: `verified_done`

## Slice

Built the missing rendered-validation path for Watch inline annotations on mobile web.

Earlier `YENTL-MOBILE-0005` verification proved the transcript and annotation controls had 44px classes, but the seeded validation routes did not render inline annotation controls at load time. This slice keeps the fix inside the Watch validation surface: `WatchView` now detects `?demo=validation&sample=...` replay routes, seeds the current playhead from the first loaded transcript segment, and seeks the media adapter to that excerpt start when ready. It does not change the corpus sample API contract and does not add a native iOS/Android claim.

## Files Changed In This Slice

- `components/session/watch-view.tsx`
- `tests/watch-view.test.tsx`
- `tests/api/corpus-sample.test.ts`

No API route implementation or shared source type contract was changed for this row.

## Pre-Fix Failure

Added a focused regression assertion for validation replay annotations, then ran:

```text
npx vitest run tests/watch-view.test.tsx -t "seeded excerpt playhead"
```

Result before implementation: failed with `Unable to find an element by: [data-testid="annotation-claim-c-at-5"]`.

## Verification

Commands:

```text
npx vitest run tests/watch-view.test.tsx tests/api/corpus-sample.test.ts
npm run build:automation
npx tsc --noEmit
```

Results:

- Focused Watch/API tests: 2 files passed, 30 tests passed.
- `npm run build:automation`: passed with webpack build and TypeScript.
- `npx tsc --noEmit`: passed after the build regenerated `.next/types`.

Note: an earlier standalone `npx tsc --noEmit` was launched while `.next/types` was missing during a concurrent build, and failed only on missing generated `.next/types/**` files. The post-build rerun passed.

## Rendered 390px Browser Proof

Route family checked on the already-running local dev server at `http://127.0.0.1:3000` with viewport `390x844`.

The API contract was separately checked with:

```text
curl http://127.0.0.1:3000/api/corpus-sample?id=solo_005
```

Result summary: `hasInitial: false`, first segment start `16.505`, claims `2`, markers `0`. This confirms the rendered playhead is not coming from a new API/source field.

Rendered measurements:

| Sample | Transcript rows | Annotation controls | Chip count | Link count | Min transcript height | Min chip height | Min chip width | Min link height | Bad targets |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| `solo_005` | 8 | 4 | 1 | 1 | 44px | 44px | 73px | 44px | 0 |
| `cable_008` | 12 | 4 | 1 | 1 | 44px | 44px | 186.87px | 44px | 0 |
| `israel_010` | 12 | 8 | 2 | 2 | 44px | 44px | 128.57px | 44px | 0 |

First rendered annotation examples:

- `solo_005`: `True98 "The world population reached 3,000,000,000 people."`
- `cable_008`: `Vagueness / Hand-waving clear "Trump is being Trump"`
- `israel_010`: `No reliable backing "The U.S. House voted today on a resolution denouncing bigotry in many forms."`

## Decision

`YENTL-MOBILE-BUILD-0003` is verified done. Future mobile audits should not reopen the Watch inline-annotation mobile target issue unless rendered evidence shows transcript rows, annotation seek chips, or annotation detail links below 44px, or validation replay routes again fail to render at least one inline annotation control.

This proves mobile-web Watch validation coverage only. It does not prove native iOS, native Android, or TV app support.
