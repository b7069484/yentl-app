# Product Roadmap Build Evidence: YENTL-PRODUCT-BUILD-0010

Timestamp: 2026-06-08T14:55:06-04:00
Loop: `product-roadmap-build`
Selected build ID: `YENTL-PRODUCT-BUILD-0010`
Outcome: built_pending_verify

## Selected Slice

Source plan/spec:

- `docs/superpowers/plans/2026-05-28-speaker-attribution-conversation-intelligence-plan.md` Phase 4
- `docs/superpowers/specs/2026-05-28-speaker-attribution-conversation-intelligence-spec.md` claim ownership semantics

Build slice:

Extend extract-claims with structured segment attribution input and optional ownership output, then persist `ClaimOwnership` on new claim cards with conservative fallback when model ownership is missing or segment attribution is unsafe.

Allowed scope:

- `lib/prompts/extract-claims.ts`
- `app/api/extract-claims/route.ts`
- `lib/client/orchestrator.ts`
- `tests/extract-claims-ownership.test.ts`
- `tests/claim-ownership-orchestrator.test.ts`

Out of scope:

- Verification prompt changes
- Provider or diarization behavior changes
- UI redesign
- Marker ownership
- Turn-builder implementation
- Staging, commits, pushes, deploys, or dependency installs

## Pre-Implementation Failure

After adding focused ownership tests, `npx vitest run tests/extract-claims-ownership.test.ts tests/claim-ownership-orchestrator.test.ts` failed as expected:

- Extracted claims did not preserve an `ownership` object.
- The extract-claims system prompt did not instruct the model to emit ownership.
- `userPrompt` did not include `SEGMENT_ATTRIBUTION`.
- `claimOwnershipForSegment` was not exported or implemented.

This confirmed the slice was real and narrow.

## Files Changed

- `lib/prompts/extract-claims.ts`
  - Added attribution status/reason/overlap/source-audio value lists.
  - Added `ExtractedClaimOwnership` schema and optional `ownership` on extracted claims.
  - Updated the system prompt to request ownership and avoid unsafe owner assignment.
  - Added a `SEGMENT_ATTRIBUTION` block to `userPrompt`.
- `app/api/extract-claims/route.ts`
  - Accepted structured segment attribution fields in the request schema.
- `lib/client/orchestrator.ts`
  - Added `claimOwnershipForSegment`.
  - Sent segment attribution metadata to `/api/extract-claims`.
  - Stored `ownership` on new `ClaimCard`s.
  - Used conservative owner fallback: unsafe overlap, quote/clip, unknown, and not-available attribution do not get a confident speaker owner.
- `tests/extract-claims-ownership.test.ts`
  - Added prompt/schema coverage for ownership output and segment attribution input.
- `tests/claim-ownership-orchestrator.test.ts`
  - Added fallback and model-ownership tests.

## Verification

- `npx vitest run tests/extract-claims-ownership.test.ts tests/claim-ownership-orchestrator.test.ts`: PASS, 2 files / 8 tests
- `npx vitest run tests/extract-claims-ownership.test.ts tests/claim-ownership-orchestrator.test.ts tests/extract-claims-schema.test.ts tests/extract-claims-stance.test.ts tests/transcript-segment-types.test.ts`: PASS, 5 files / 26 tests
- `npx tsc --noEmit`: PASS
- `npm run test:run`: PASS, 128 files / 1384 tests
- `npm run build`: PASS

Notes:

- Full test output still includes repeated jsdom warnings that `HTMLMediaElement.pause()` is not implemented. These warnings remain non-fatal; all tests passed.
- The build completed with Next.js 16.2.6 / Turbopack and generated 39 static pages.

## Ledger Updates

- `YENTL-PRODUCT-BUILD-0009` moved from `built_pending_verify` to `verified_done` after independent schema tests, typecheck, full tests, and build passed.
- `YENTL-PRODUCT-BUILD-0010` added as `built_pending_verify`.

## Next Action

A later watchdog/audit pass should independently rerun ownership tests, typecheck, full test suite, and build before marking `YENTL-PRODUCT-BUILD-0010` as `verified_done`.

Useful follow-on slices:

- Carry ownership into provisional/confirmed verification prompts or synthesis inputs.
- Surface ownership/uncertainty lines on claim cards.
- Add marker ownership schema/output in the rhetoric lane.
- Start a bounded turn-builder slice for clean solo/interview/backchannel cases.
