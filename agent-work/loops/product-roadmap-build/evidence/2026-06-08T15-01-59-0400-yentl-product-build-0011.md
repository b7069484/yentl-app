# Product Roadmap Build Evidence: YENTL-PRODUCT-BUILD-0011

Timestamp: 2026-06-08T15:01:59-04:00
Loop: `product-roadmap-build`
Selected build ID: `YENTL-PRODUCT-BUILD-0011`
Outcome: built_pending_verify

## Selected Slice

Source plan/spec:

- `docs/superpowers/plans/2026-05-28-speaker-attribution-conversation-intelligence-plan.md` Phase 4
- User objective: keep the meta-view of a whole conversation/article sharp and ownership-aware

Build slice:

Feed ownership-aware claim summaries into `/api/synthesize` so synthesis can judge per-speaker verdicts, topic concentration, and uncertain/quoted ownership from actual claim evidence instead of aggregate counters alone.

Allowed scope:

- `lib/prompts/synthesize.ts`
- `lib/client/orchestrator.ts`
- `tests/synthesis-ownership-context.test.ts`

Out of scope:

- Verification prompt changes
- Devil's Advocate changes
- Claim-card UI rendering
- Export/report rendering
- Provider or diarization behavior changes
- Staging, commits, pushes, deploys, or dependency installs

## Pre-Implementation Failure

After adding focused synthesis ownership tests, `npx vitest run tests/synthesis-ownership-context.test.ts` failed as expected:

- `SynthesizeRequest` did not preserve a `claims` array.
- `userPrompt` did not contain a `CLAIMS` section.
- `runFinalSynthesis` did not send claim summaries to `/api/synthesize`.

This confirmed the slice was real and narrow.

## Files Changed

- `lib/prompts/synthesize.ts`
  - Added a claim-summary schema with text, verdict, score, owner, topic, stance, attribution status/reasons, and explanation.
  - Added optional/default `claims` to `SynthesizeRequest`.
  - Added a `CLAIMS` section to `userPrompt`.
  - Updated synthesis instructions to use claim evidence for speaker verdicts and not count uncertain/quoted/reported/null-owner claims as clean assertions.
- `lib/client/orchestrator.ts`
  - Added claim summaries for synthesis from current `ClaimCard`s.
  - Sent claim summaries with `/api/synthesize` requests.
- `tests/synthesis-ownership-context.test.ts`
  - Added schema/prompt coverage.
  - Added orchestrator payload coverage for ownership-aware synthesis calls.

## Verification

- `npx vitest run tests/synthesis-ownership-context.test.ts`: PASS, 1 file / 3 tests
- `npx vitest run tests/synthesis-ownership-context.test.ts tests/synthesize-route.test.ts tests/synthesis-pacer.test.ts tests/end-session-synthesis.test.ts`: PASS, 4 files / 36 tests
- `npx tsc --noEmit`: PASS
- `npm run test:run`: PASS, 129 files / 1387 tests
- `npm run build`: PASS

Notes:

- Earlier in this run, a parallel `npx tsc --noEmit` overlapped with `npm run build` and hit a transient `.next/types/routes.d.ts is not a module` race. Rerunning typecheck after build completed passed.
- Full test output still includes repeated jsdom warnings that `HTMLMediaElement.pause()` is not implemented. These warnings remain non-fatal; all tests passed.
- The build completed with Next.js 16.2.6 / Turbopack and generated 39 static pages.

## Ledger Updates

- `YENTL-PRODUCT-BUILD-0010` moved from `built_pending_verify` to `verified_done` after independent ownership tests, typecheck, full tests, and build passed.
- `YENTL-PRODUCT-BUILD-0011` added as `built_pending_verify`.

## Next Action

A later watchdog/audit pass should independently rerun synthesis ownership tests, typecheck, full test suite, and build before marking `YENTL-PRODUCT-BUILD-0011` as `verified_done`.

Useful follow-on slices:

- Carry ownership into provisional/confirmed verification prompts.
- Carry ownership into Devil's Advocate prompts.
- Surface claim ownership/uncertainty on claim cards and exports.
- Start a bounded turn-builder slice for clean solo/interview/backchannel cases.
