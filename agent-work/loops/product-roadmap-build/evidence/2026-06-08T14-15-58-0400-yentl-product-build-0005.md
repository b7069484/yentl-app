# Product Roadmap Build Report: YENTL-PRODUCT-BUILD-0005

Timestamp: 2026-06-08T14:15:58-04:00

## Outcome

Status: `built_pending_verify`.

## Selected Build Slice

- ID: `YENTL-PRODUCT-BUILD-0005`
- Source plan: `docs/superpowers/plans/2026-05-28-yentl-launch-foundation-phase-1a.md`, Task 5.
- Slice: make live Deepgram speaker attribution confidence-weighted, preserve live word/provider/source metadata, and flag latent speaker-change boundaries.

## Files Changed

- `lib/client/deepgram-stream.ts`
- `lib/types.ts`
- `tests/dominant-speaker-confidence.test.ts`
- `tests/diarization.test.ts`
- `agent-work/loops/build-ledger.md`

## What Changed

- Added `dominantSpeaker(words: ASRWord[] | undefined)` scoring by `word duration * speaker_confidence` instead of word count.
- Return `null` when no speaker evidence exists or the top speaker margin is under 10 percent of total weighted speaker evidence.
- Added `isLatentBoundary(segment, prior)` for segments starting within 300 ms of the prior segment end.
- Live Deepgram final segments now include stable-ish IDs, provider metadata, normalized `words[]`, `source_audio_kind: "mic"`, attribution status/reasons, and `latent_boundary` when applicable.
- Preserved `diarize=false`; this run does not enable voiceprint/speaker segmentation or change consent-sensitive Deepgram parameters.
- Added focused confidence/boundary regression coverage and updated existing diarization helper tests to use `ASRWord`.

## Verification

- Expected failing test before implementation: `npx vitest run tests/dominant-speaker-confidence.test.ts tests/diarization.test.ts` failed 4 tests for old word-count selection, missing low-margin null behavior, and missing `isLatentBoundary`.
- `npx vitest run tests/dominant-speaker-confidence.test.ts tests/diarization.test.ts`: PASS, 2 test files and 14 tests passed.
- `npx vitest run tests/extract-claims-stance.test.ts tests/extract-claims-schema.test.ts`: PASS, 2 test files and 9 tests passed.
- `npx tsc --noEmit`: PASS.
- `npm run test:run`: PASS, 122 test files and 1362 tests passed. Existing jsdom `HTMLMediaElement.pause()` warnings appeared but did not fail the run.
- `npm run build`: PASS.

## Independent Verification Of Prior Phase 1a Row

- `YENTL-PRODUCT-BUILD-0004`: verified by rerunning the stance/schema tests after later product changes, plus typecheck, full tests, and build.

## Notes

- Live streams with words but no speaker tags now remain `not_available` with `provider_missing_speaker`, which matches `diarize=false`.
- Live streams with speaker tags but weak margin are `uncertain` with `dominant_speaker_low_margin`.
- The stream source metadata still defaults to `mic` per Phase 1a. A later source-routing slice should pass `browser_tab` or `youtube_caption` where upstream capture paths know the source.

## Next Action

A later audit/watchdog pass should independently verify `YENTL-PRODUCT-BUILD-0005` before marking it `verified_done`. The next product slice should add source-specific live stream options or move to the next Phase 1a task from the launch foundation plan.
