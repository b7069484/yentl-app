# P0-001 Staging Plan - document-ingest prod 500 hotfix

Operator: codex-yentl  
Readiness: claude-yentl  
Owner: Israel  
Created: 2026-06-19T23:44:51Z

## Current Dirty Tree

- Current `git status --porcelain=v1`: 258 rows.
- Tracked modified files: 149.
- Untracked files: 135.
- Conclusion: the tree is larger than the audit snapshot and is not safe to push as one dump.

## Candidate Deploy Set A - minimal production hotfix

Runtime files to stage only after Readiness reviews this plan and P0-002 is verified:

- `app/api/document-ingest/route.ts`
- `next.config.ts`
- `lib/server/security-headers.ts`

Focused tests to stage with the runtime change:

- `tests/api/document-ingest.test.ts`
- `tests/security-headers.test.ts`

Coordination/control artifacts to stage with the deploy PR/commit, because they are non-runtime and record the gate:

- `agent-comms/PROTOCOL.md`
- `agent-comms/BOARD_SCHEMA.md`
- `agent-comms/inbox.mjs`
- `agent-comms/notify.mjs`
- `docs/launch-gate/yentl-readiness-board.json`
- `docs/launch-gate/yentl-readiness-criteria.claude.json`
- `docs/launch-gate/2026-06-19-p0-001-staging-plan.md`

Do not stage the mailbox file by default:

- `agent-comms/yentl-mailbox.json`

Rationale: the mailbox is a local append-only coordination channel with read-state metadata. It can stay local unless Owner explicitly wants it versioned.

## Why This Set

- `document-ingest` currently 500s on prod because the PDF worker is not available in the deployed serverless bundle. The dirty tree changes configure `pdf-parse` with a base64 data URL from `node_modules/pdf-parse/dist/worker/pdf.worker.mjs`.
- `next.config.ts` adds the file trace entry for `/api/document-ingest` so the worker file is bundled into the Vercel function.
- `next.config.ts` also wires the Readiness-verified P1-001 security headers through static `headers()` with no request-routing behavior change.
- The set is small enough for Readiness to review and reproduce. It does not include unrelated UX, extension, proof-battery, validation-fixture, speaker-attribution, or broad product changes.

## Explicit Exclusions From P0-001 Hotfix

- All P1-002 verdict-vocabulary and contrast work, including `lib/client/verdict-theme.ts`, `components/session/chips.tsx`, `components/session/claim-row.tsx`, `components/session/claim-detail.tsx`, and `components/session/MarkerChip.tsx`. Readiness/Grok owns that isolated worktree.
- Dirty UI/product files such as `app/mobile/page.tsx`, `app/tv/page.tsx`, `app/session/page.tsx`, `app/sessions/page.tsx`, and `components/session/**`.
- Extension files and assets under `extension/**`.
- Proof JSON churn under `docs/superpowers/validation/**`, especially `ingestion-deploy-proof.json`, which still needs P2-001 honesty cleanup before it can be trusted as a deploy signal.
- Validation fixture expansion under `lib/server/validation-*.ts`, `public/validation/**`, and related proof scripts.
- P0-004 SSRF work. Current dirty code still has the transcribe-batch JSON `{ url }` branch calling `transcribeUrl(targetUrl)` without `assertSafeUrl`, and media/article server fetch paths still have the DNS-rebind TOCTOU risk. That stays a separate P0 row.
- P0-003 YouTube prod datacenter-IP fix. No dirty code in this plan resolves `{code:PRIVATE}` from Vercel IPs.
- P0-005 security-event sink and engagement-gate resilience. No dirty code in this plan makes security events persistent or changes the model-error fail mode.

## Pre-Push Verification Required

Run from the staged tree before any Owner-gated push:

- `npx vitest run tests/api/document-ingest.test.ts tests/security-headers.test.ts`
- `npx tsc --noEmit`
- `npm run build:automation`
- Local document-ingest curl against the validation PDF returns 200 with extracted text.

Gate condition before deploy:

- P0-002 must be verified or explicitly cleared by Readiness: live AI Gateway model slug works, prod unauth paid route returns 401, and prod `/api/corpus-sample` returns 404.

## Post-Deploy Proof Required

Only after Owner approves push/deploy:

- `curl` prod `/api/document-ingest` with `public/validation/yentl-small-text-layer.pdf` and source-analysis consent returns 200 with extracted text.
- Prod `/`, `/session`, and representative API routes render/respond 200.
- Prod security headers show the five P1-001 headers on page and API responses.
- Post-deploy ingestion/analysis/a11y/trust battery is green or records real blockers as blockers, not hidden `ok:true`.

## Rollback

- Vercel instant rollback to prior production deployment `cda517a`.
- Code rollback is limited to the three runtime files above if this set is staged as planned.

## Topology Update

Readiness checkpointed the full pre-launch tree locally as `4e89517` on `main` after this plan was first posted. `origin/main` remains `cda517a`; `main` is push-protected because pushing it is the production deploy key. The minimal set above remains the Operator's preferred safer staging shape, but the live local `main` checkpoint is a full-tree checkpoint. Follow-on Operator code work goes in `/Users/israelbitton/yentl-codex-launch-gate` on `codex/launch-gate`, not in the main checkout.

## Next Single Action

Readiness reviews this plan, then Operator runs the pre-push verification and P0-002 probes. No push or deploy occurs until the gate sequence clears.
