# Yentl UI/mobile audit report

Date: 2026-06-07T17:47:59-04:00
Workspace: `/Users/israelbitton/Live FactCheck`
Loop: `ui-mobile-audit`
Mode: pilot audit-only

## Repo truth

- `git status --short --branch`: `main...origin/main [behind 11]` with untracked loop/docs/agent-work files already present.
- `git diff --stat`: no tracked diff output.
- Local browser verification against `http://localhost:3000/` was blocked by browser security policy, so this run uses code inspection plus prior captured evidence only.
- `npm run build` did not complete. Next 16/Turbopack failed with `components/BorderGlow.css` and `Operation not permitted (os error 1)` while creating a new process/binding to a port.

## Prior evidence consulted

- `agent-work/hadassah-mobile-a11y/mobile-a11y-report.md`
- `agent-work/shira-source-intake/before-after-notes.md`
- `agent-work/lev-signal-system/signal-language.md`
- `agent-work/miriam-flow-atlas/coverage-audit.md`

## Desktop findings

1. High: `/privacy` claims that Yentl "supports Global Privacy Control (GPC) signals", but repo search found no implementation path, header handling, or runtime detection for GPC. This is an unsupported public legal claim.
   - Evidence: `app/privacy/page.tsx:234-242`
   - Verification: `rg -n "Global Privacy Control|GPC|privacy control|navigator.globalPrivacyControl|Sec-GPC" -S app components lib`
   - Ledger: added `YENTL-TRUTH-0001`
2. High: `/accessibility` claims "Substantial conformance", "No content breaks at 200% zoom", "All interactive targets ≥44×44 CSS px", and "Navigation structure consistent across pages", but this run could not verify those assertions and the public route set currently spans mixed shells/patterns. The copy overstates audited certainty.
   - Evidence: `app/accessibility/page.tsx:26-58`, `app/accessibility/page.tsx:94-113`
   - Ledger: added `YENTL-TRUTH-0002`
3. Medium: several trust/legal routes still use an older document shell instead of the refreshed public launch shell. They lack the stronger home/session return affordances used on `/pricing`, `/faq`, and `/demo`, which increases mobile dead-end risk and weakens launch consistency.
   - Evidence: `app/accessibility/page.tsx:14-136`, `app/contact/page.tsx:11-63`, `app/privacy/page.tsx:14-260`, `app/subprocessors/page.tsx:14-105`, `app/terms/page.tsx:13-149`
   - Ledger: added `YENTL-UI-0001`

## Mobile findings

- Prior session/mobile evidence still points to improved session tabs, 44px controls, and better ingest-state clarity on `/session`; this run found no new code evidence contradicting those earlier fixes.
- New mobile risk is concentrated in trust/legal routes, where the older document shell provides weaker return navigation and less product-context continuity than the refreshed launch pages.
- Source-picker platform messaging remains honest in code: mobile variants explicitly avoid promising arbitrary tab capture and route users toward share/import/mic paths.
  - Evidence: `components/session/source-picker.tsx:197-240`

## Platform-truth findings

- Browser-tab capture is still described honestly as a desktop Chrome extension path in the source picker and ingest pane.
  - Evidence: `components/session/source-picker.tsx:199-210`, `components/session/ingest-panes/browser-tab-ingest-pane.tsx:63-183`
- Homepage/session copy remains aligned with guest-first, browser-local saves.
  - Evidence: `app/page.tsx:152-180`, `app/faq/page.tsx`, `app/pricing/page.tsx`
- The strongest truth gap is legal/privacy copy that asserts support or audit coverage beyond what this repo currently proves.

## Public-copy risks

- Launch-critical: unsupported GPC support claim on `/privacy`.
- High-risk: accessibility statement presents strong conformance assertions as settled fact even though this run could not reproduce route-wide proof and the current public pages are not on one consistent shell.
- Lower-risk but notable: legal/trust pages visually read like a different product generation than the refreshed home/demo/pricing/faq surfaces.

## Ranked fixes

1. `YENTL-TRUTH-0001` `ready_for_fix`
   - Remove or narrow the GPC support claim in `app/privacy/page.tsx`.
   - Verification: repo search still shows no GPC implementation references after the copy change.
2. `YENTL-TRUTH-0002` `ready_for_fix`
   - Narrow `/accessibility` copy so it states target level and limited verified audit scope without asserting broad conformance guarantees the repo cannot currently prove.
   - Verification: statement text matches the actual audited surfaces/scope.
3. `YENTL-UI-0001` `new`
   - Unify trust/legal routes onto the current public shell or add a shared trust-page wrapper with strong return navigation and consistent launch styling.
   - Verification: route family review across `/privacy`, `/terms`, `/subprocessors`, `/accessibility`, `/contact`.

## Ledger updates made

- Added `YENTL-TRUTH-0001` as new `ready_for_fix`.
- Added `YENTL-TRUTH-0002` as new `ready_for_fix`.
- Added `YENTL-UI-0001` as new `new`.
- No duplicates, reopenings, or verified-fixed rows were applicable because this was the first run.

## Recommended next action

Fix one narrow truth issue first: start with `YENTL-TRUTH-0001` on `/privacy`, then rerun the audit.
