# Yentl UI/mobile audit report

Date: 2026-06-07T23:01:49-04:00
Workspace: `/Users/israelbitton/Live FactCheck`
Loop: `ui-mobile-audit`
Mode: scheduled pilot audit-only

## Repo truth

- `git status --short --branch`: `main...origin/main [behind 11]` with pre-existing untracked `agent-work/loops/`, Yentl agent-work folders, `docs/ops/`, launch plan/spec docs, and `scripts/experiments/`.
- `git diff --stat`: no tracked diff output before this run's permitted loop-artifact edits.
- `npm run build`: blocked by a Turbopack sandbox panic before app-level verification. Exact blocker: `[project]/components/BorderGlow.css [app-client] (css)` caused by `creating new process`, `binding to a port`, `Operation not permitted (os error 1)`. Panic log path: `/var/folders/y7/rjg40xdj29q391m6txgpjt1r0000gn/T/next-panic-439484854d4ba0e3df584d797ec14ce2.log`.
- Product code was not edited.

## Prior evidence consulted

- `agent-work/hadassah-mobile-a11y/mobile-a11y-report.md`: prior mobile session screenshots at 390 x 844 showed session tabs, header controls, transcript, claims, and markers fitting without horizontal overflow.
- `agent-work/shira-source-intake/before-after-notes.md`: prior desktop/mobile intake screenshots covered audio, media URL, mic, text, and browser-tab waiting states.
- `agent-work/lev-signal-system/signal-language.md`: prior Watch and extension signal language evidence covered desktop, side-panel, and mobile signal surfaces.
- `agent-work/miriam-flow-atlas/coverage-audit.md`: prior `/project/flows` evidence classified many flow states as missing targets and confirmed the route is an internal planning atlas.

## Desktop findings

1. High: `YENTL-TRUTH-0001` recurs. `/privacy` still states that Yentl supports Global Privacy Control signals, while repo search still finds no runtime/header implementation outside this public copy.
   - Evidence: `app/privacy/page.tsx:230-244`; search pattern `Global Privacy Control|GPC|privacy control|navigator.globalPrivacyControl|Sec-GPC`.
   - Ledger decision: duplicate of existing row; kept `blocked_needs_context`; recurrence incremented to 3.
2. High: `YENTL-TRUTH-0002` recurs. `/accessibility` still asserts substantial conformance, all 44x44 targets, clean axe audits, and Lighthouse tooling; `/about` still says substantial compliance and that automated axe-core plus Lighthouse audits run in CI. CI still gates the axe audit on `vars.RUN_A11Y_AUDIT == 'true'`.
   - Evidence: `app/accessibility/page.tsx:26-58`; `app/accessibility/page.tsx:94-113`; `app/about/page.tsx:130-137`; `.github/workflows/ci.yml:35-42`.
   - Ledger decision: duplicate of existing row; kept `ready_for_fix`; recurrence incremented to 3.
3. Medium: `YENTL-UI-0001` recurs. Older document-shell routes still differ from the refreshed launch shell and weaker return navigation remains visible in code.
   - Evidence: `app/about/page.tsx:11-153`; `app/methodology/page.tsx:11-168`; `app/privacy/page.tsx:14-278`; `app/terms/page.tsx:13-149`; `app/subprocessors/page.tsx:14-106`; `app/accessibility/page.tsx:14-136`; `app/contact/page.tsx:11-63`.
   - Ledger decision: duplicate of existing row; kept `new` because the route-family wrapper decision is not narrow enough for the fix loop; recurrence incremented to 3.

## Mobile findings

1. Medium: new `YENTL-MOBILE-0001`. Several `/session` ingest-pane "Back to sources" controls are coded as compact text buttons with no `min-h-11` or equivalent 44px mobile target, while primary controls in the same flow already use `min-h-11` or `min-h-12`.
   - Evidence: `components/session/ingest-panes/audio-ingest-pane.tsx:226-232`; `components/session/ingest-panes/browser-tab-ingest-pane.tsx:63-69`; `components/session/ingest-panes/mic-prerecord-pane.tsx:68-74`; `components/session/ingest-panes/text-ingest-pane.tsx:236-242`; `components/session/ingest-panes/media-url-ingest-pane.tsx:238-244`; `components/session/ingest-panes/web-url-ingest-pane.tsx:204-210`; `components/session/ingest-panes/claim-quick-check-pane.tsx:152-158`; `components/session/ingest-panes/youtube-ingest-pane.tsx:1065-1072`.
   - Ledger decision: new row added and marked `ready_for_fix` because the scope is narrow, non-legal, and verifiable.
2. Existing session shell mobile structure still looks code-safe: tabs use a 2/3-column mobile grid and top controls use 44px mobile heights.
   - Evidence: `components/session/session-shell.tsx:143-161`; `components/session/session-shell.tsx:220-276`.
3. No fresh screenshot proof was captured this run. Mobile behavior above is code-read plus prior Hadassah/Shira screenshots, not fresh Browser/device proof.

## Platform-truth findings

- Source selection remains platform-honest. Mobile platforms omit current-tab capture and present shared link, file, text, and microphone paths with explicit copy that mobile browsers cannot expose another tab's audio like desktop Chrome.
  - Evidence: `components/session/source-picker.tsx:65-81`; `components/session/source-picker.tsx:197-240`; `components/session/source-picker.tsx:304-416`.
- Browser-tab capture remains framed as a desktop Chrome extension path in the source picker and browser-tab pane.
  - Evidence: `components/session/source-picker.tsx:197-210`; `components/session/ingest-panes/browser-tab-ingest-pane.tsx:77-185`.
- FAQ, pricing, demo, and sessions copy remain aligned with guest-first, browser-local saves and avoid account sync or paid-plan overpromises.
  - Evidence: `app/faq/page.tsx:17-41`; `app/pricing/page.tsx:11-122`; `app/demo/page.tsx:18-29`; `app/sessions/page.tsx:345-355`; `app/sessions/page.tsx:390-391`.
- `/project/flows` remains labeled as an internal planning route, not an end-user launch promise.
  - Evidence: `app/project/flows/page.tsx:11-22`; `agent-work/miriam-flow-atlas/coverage-audit.md`.

## Public-copy risks

- Launch-critical: unsupported GPC support claim remains on `/privacy`; still gated for legal/human approval rather than normal fix-loop routing.
- High-risk: accessibility conformance/audit certainty remains overstated on `/accessibility` and `/about`; this is the next normal fix-loop candidate.
- Medium-risk: public informational routes still use an older shell family; the finding remains too broad for a narrow fix-loop item until a shared wrapper decision is made.

## Ranked fixes

1. `YENTL-TRUTH-0002` - ready for normal fix loop.
   - Narrow accessibility copy in `app/accessibility/page.tsx:26-58`, `app/accessibility/page.tsx:94-113`, and `app/about/page.tsx:130-137` to audited scope and conditional CI reality.
   - Verification: copy should distinguish target level, implemented features, conditional automated checks, and unverified manual/screenshot scope.
2. `YENTL-MOBILE-0001` - ready for normal fix loop.
   - Give ingest-pane back/source-return controls a `min-h-11` target while preserving compact visual placement.
   - Verification: code review plus targeted mobile render check for all affected ingest panes; back controls should be at least 44px high.
3. `YENTL-TRUTH-0001` - blocked, not normal fix-loop work.
   - Needs human/legal approval or a dedicated legal-copy lane before changing `/privacy` GPC language.
4. `YENTL-UI-0001` - keep as backlog/new.
   - Decide whether to introduce a shared public information/trust wrapper before route-by-route cleanup.

## Ledger updates made

- `YENTL-TRUTH-0001`: recurrence incremented from 2 to 3; kept `blocked_needs_context`.
- `YENTL-TRUTH-0002`: recurrence incremented from 2 to 3; kept `ready_for_fix`.
- `YENTL-UI-0001`: recurrence incremented from 2 to 3; kept `new`.
- Added `YENTL-MOBILE-0001` as `ready_for_fix`.
- No `fixed_pending_verify` rows were present, so no rows were marked `verified_fixed` or `reopened`.

## Recommended next action

Fix one narrow issue: start with `YENTL-TRUTH-0002` accessibility/about copy, while keeping `YENTL-TRUTH-0001` gated for human/legal approval.
