# Product Roadmap Build Evidence - 2026-06-08T17:53:04-04:00

Loop: `agent-work/loops/product-roadmap-build`  
Mode: interactive non-overlapping product slice while the immediate wave waits for the scheduled 6:30 PM `YENTL-PRODUCT-BUILD-0016` sidecar row.

## Selected Build

- ID: `YENTL-PRODUCT-BUILD-0017`
- Status after this run: `built_pending_verify`
- Product area: Marker attribution context
- Source plan: `docs/superpowers/plans/2026-05-28-speaker-attribution-conversation-intelligence-plan.md` Phase 5

## Scope Guard

This pass did not touch the scheduled `YENTL-PRODUCT-BUILD-0016` sidecars, `YENTL-UI-ROADMAP-0002`, or `YENTL-MOBILE-BUILD-0002`.

Allowed product scope for this row was the rhetoric marker path:

- `lib/prompts/analyze-rhetoric.ts`
- `lib/client/orchestrator.ts`
- one focused test file

## Files Changed

- `lib/prompts/analyze-rhetoric.ts`
  - Added optional marker attribution fields to the output schema: `attribution_status`, `attribution_reasons`, `overlap_class`, `source_turn_ids`, and `source_segment_ids`.
  - Updated the system prompt to use attribution/floor-state metadata when present and to avoid inventing turn or segment ids.
- `lib/client/orchestrator.ts`
  - Added `formatRhetoricTranscriptLine()` and `rhetoricTranscriptWindowForSegments()` so `/api/analyze-rhetoric` receives speaker, attribution, overlap, turn, and segment metadata.
  - Added `markerAttributionForSpan()` so markers get safe fallback attribution metadata from overlapping transcript segments when the model omits it.
  - Preserved `attributeMarker()` as the fallback for `speaker_id`.
- `tests/analyze-rhetoric-attribution-context.test.ts`
  - Covered metadata-rich transcript lines, window filtering, unsafe overlap fallback metadata, and marker schema acceptance of optional attribution ids.

## Verification

- `npx vitest run tests/analyze-rhetoric-attribution-context.test.ts tests/analyze-rhetoric-cache.test.ts`: pass, 2 files / 5 tests.
- `npx vitest run tests/transcript-segment-types.test.ts tests/session-store.test.ts tests/filtered-list.test.tsx tests/item-detail.test.tsx tests/learn-more.test.tsx`: pass, 5 files / 87 tests.
- `npx tsc --noEmit`: pass.
- `npm run build`: pass, Next.js generated 39 static pages.
- `npm run test:run`: pass, 133 files / 1403 tests.

## Result

The rhetoric marker path no longer receives plain timestamped text only. It now receives attribution-aware transcript context and persists optional marker attribution metadata without requiring a UI redesign, provider change, diarization enablement, taxonomy expansion, or legal/consent change.

This does not prove marker-owner accuracy yet. The hard-window report still needs marker sidecars and scoring before any robust marker-attribution claim is allowed.

## Next State

- Move `YENTL-PRODUCT-BUILD-0017` to `built_pending_verify`.
- Leave `YENTL-PRODUCT-BUILD-0016` as `ready_for_build` for the scheduled 6:30 PM product-roadmap worker.
- Later control/watchdog/product verification should independently rerun the focused rhetoric marker tests, typecheck, and report whether `0017` can become `verified_done`.
