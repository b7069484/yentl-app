# YENTL-MOBILE-BUILD-0004

Timestamp: 2026-06-08T23:12:34-04:00
Lane: mobile-ui-build
Result: verified_done

## Scope

Consumed the exact `ready_for_build` row `YENTL-MOBILE-BUILD-0004`: populated mobile-web annotation detail route handoff from Watch inline annotations.

Allowed files touched:

- `components/session/item-detail.tsx`
- `components/session/claim-detail.tsx`
- `components/session/marker-detail.tsx`
- `tests/item-detail.test.tsx`

No API, legal/privacy copy, source-capture policy, native iOS/Android implementation, TV app implementation, broad Watch redesign, dependency install, git staging, commit, push, or deploy was performed.

## What changed

- Added 44px-class guards and stable test hooks to the detail breadcrumb/back control, previous/next sibling controls, and NotFound return control.
- Added 44px-class guards and stable hooks to claim/marker detail action buttons and footer learning/transcript links.
- Added focused `ItemDetail` tests for claim detail, marker detail, and missing-item mobile tap-target guards.

## Pre-fix proof

`npx vitest run tests/item-detail.test.tsx -t "mobile detail tap targets"` failed before implementation because:

- `detail-back-btn` did not exist.
- `detail-notfound-back-btn` did not exist.
- Existing previous/next controls were `w-6 h-6`, below the 44px mobile target.
- Action and footer controls had no `min-h-11` guard.

## Verification

Passed:

- `npx vitest run tests/item-detail.test.tsx -t "mobile detail tap targets"`: 3 passed.
- `npx vitest run tests/item-detail.test.tsx`: 23 passed.
- `npm run build:automation`: passed, including Next TypeScript step and all 39 app routes.
- `npx tsc --noEmit`: passed after `build:automation` regenerated `.next/types`.

Note: the first standalone `npx tsc --noEmit` attempt ran before `.next/types` had been regenerated and failed only on missing generated Next type files. `npm run build:automation` regenerated those files and passed TypeScript; the post-build standalone `npx tsc --noEmit` then passed.

## Rendered 390px proof

Browser viewport: 390x844.

Claim route:

- URL: `http://localhost:3000/session/detail/claim/solo_005-claim-1?demo=validation&sample=solo_005`
- Populated content found: `world population reached 3,000,000,000 people`
- NotFound copy absent.
- Horizontal overflow: false (`docWidth=390`, `viewportWidth=390`).
- Bad measured targets below 44px: 0.
- Measured visible target heights:
  - `detail-back-btn`: 44px
  - `detail-prev-btn`: 44px
  - `detail-next-btn`: 44px
  - `detail-action-btn` controls: 44px each
  - `claim-transcript-link`: 44px
  - `claim-learn-link`: 44px

Marker route:

- URL: `http://localhost:3000/session/detail/marker/cable_008-marker-1?demo=validation&sample=cable_008`
- Populated content found: `Vagueness / Hand-waving`
- NotFound copy absent.
- Horizontal overflow: false (`docWidth=390`, `viewportWidth=390`).
- Bad measured targets below 44px: 0.
- Measured visible target heights:
  - `detail-back-btn`: 44px
  - `detail-prev-btn`: 44px
  - `detail-next-btn`: 44px
  - `detail-action-btn` controls: 44px each
  - `marker-learn-link`: 44px

## Current truth

`YENTL-MOBILE-BUILD-0004` is verified for mobile-web annotation detail routes only. This does not prove native iOS, native Android, TV, legal/privacy copy, or broader Watch redesign readiness.
