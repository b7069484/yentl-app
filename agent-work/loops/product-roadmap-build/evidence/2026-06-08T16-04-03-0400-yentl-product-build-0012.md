# YENTL-PRODUCT-BUILD-0012 Evidence

Timestamp: 2026-06-08T16:04:03-04:00
Lane: product-roadmap-build
Status: built_pending_verify

## Slice

Devil's Advocate now receives ownership-aware claim summaries and explicit prompt guidance so skeptical analysis does not treat quoted, reported, denied, mocked, questioned, uncertain, unsafe, or clip-sourced claims as confident direct assertions by the visible speaker.

This carries the speaker-attribution / conversation-intelligence work past extraction and synthesis into the adversarial meta-view.

## Files Changed

- `lib/prompts/devil-advocate.ts`
- `lib/client/orchestrator.ts`
- `tests/devil-advocate-ownership-context.test.ts`
- `agent-work/loops/build-ledger.md`
- `agent-work/loops/product-roadmap-build/STATE.md`
- `agent-work/loops/product-roadmap-build/alerts.md`

## Pre-Implementation Failure

Command:

```sh
npx vitest run tests/devil-advocate-ownership-context.test.ts
```

Result: failed as expected.

- Schema stripped `stance`, `attribution_status`, and `attribution_reasons`.
- Prompt rendered `[Unknown]` and omitted topic/stance/attribution context.
- Orchestrator `/api/devil-advocate` payload omitted ownership context.

## Implementation

- Extended `DevilAdvocateRequest.claims[]` with optional `topic`, `stance`, `attribution_status`, and defaulted `attribution_reasons`.
- Added Devil's Advocate system rules that require stance/attribution handling before assigning claim responsibility.
- Rendered claim rows with owner, topic, stance, attribution status, and attribution reasons.
- Shared the orchestrator claim-summary mapper so both synthesis and Devil's Advocate use `claim.ownership?.owner_speaker_id`, stance, attribution status/reasons, topic, score, verdict, and explanation.

## Verification

```sh
npx vitest run tests/devil-advocate-ownership-context.test.ts
```

PASS: 1 file, 3 tests.

```sh
npx vitest run tests/devil-advocate-route.test.ts
```

PASS: 1 file, 5 tests.

```sh
npx vitest run tests/synthesis-ownership-context.test.ts tests/end-session-synthesis.test.ts
```

PASS: 2 files, 9 tests.

```sh
npx tsc --noEmit
```

PASS.

```sh
npm run test:run
```

PASS: 130 files, 1390 tests.

```sh
npm run build
```

PASS: Next.js 16.2.6, 39 static pages generated.

## Ledger Impact

- `YENTL-PRODUCT-BUILD-0011` is independently verified and can move to `verified_done`.
- `YENTL-PRODUCT-BUILD-0012` is built pending later watchdog/audit verification.

## Safety

- No staging, commit, push, deploy, dependency install, destructive git command, production diarization change, provider change, verification prompt change, or UI redesign.
- Existing dirty checkout and untracked loop/plan/experiment folders were preserved.
