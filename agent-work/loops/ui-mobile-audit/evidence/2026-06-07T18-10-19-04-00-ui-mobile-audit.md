# Yentl UI/mobile audit report

Date: 2026-06-07T18:10:19-04:00
Workspace: `/Users/israelbitton/Live FactCheck`
Loop: `ui-mobile-audit`
Mode: manual canary, pilot audit-only

## Repo truth

- `git status --short --branch`: `main...origin/main [behind 11]` with pre-existing untracked `agent-work/loops/`, Yentl agent-work folders, `docs/ops/`, launch plan/spec docs, and `scripts/experiments/`.
- `git diff --stat`: no tracked diff output before this run's permitted loop-artifact edits.
- `npm run build`: passed. Next 16.2.6 compiled, TypeScript completed, and 39 static pages generated.
- Local preview: `npm run dev -- --hostname 127.0.0.1 --port 3000` started and was stopped in this turn. Visual Browser verification was blocked because the in-app Browser surface was unavailable (`iab` not available), so desktop/mobile behavior below is code-read plus prior evidence, not fresh screenshot proof.
- Product code was not edited.

## Prior evidence consulted

- `agent-work/hadassah-mobile-a11y/mobile-a11y-report.md`: prior mobile session screenshots at 390 x 844 showed session tabs, header controls, transcript, claims, and markers fitting without horizontal overflow.
- `agent-work/shira-source-intake/before-after-notes.md`: prior desktop/mobile intake screenshots covered audio, media URL, mic, text, and browser-tab waiting states.
- `agent-work/lev-signal-system/signal-language.md`: prior Watch and extension signal language evidence covered desktop, side-panel, and mobile signal surfaces.
- `agent-work/miriam-flow-atlas/coverage-audit.md`: prior `/project/flows` evidence classified many flow states as missing targets and confirmed the route is an internal planning atlas.

## Desktop findings

1. High: `YENTL-TRUTH-0001` recurs. `/privacy` still states that Yentl supports Global Privacy Control signals, while repo search still finds no runtime/header implementation outside this public copy.
   - Evidence: `app/privacy/page.tsx:230-244`; search pattern `Global Privacy Control|GPC|privacy control|navigator.globalPrivacyControl|Sec-GPC`.
   - Decision: duplicate of existing ledger row; recurrence incremented.
2. High: `YENTL-TRUTH-0002` recurs and now includes `/about`. `/accessibility` still asserts substantial conformance, 200% zoom safety, all 44x44 targets, and consistent navigation; `/about` repeats substantial compliance and says full axe/Lighthouse audits run in CI. The CI workflow shows the axe step is conditional on `vars.RUN_A11Y_AUDIT == 'true'`, so the public copy overstates what this run can prove.
   - Evidence: `app/accessibility/page.tsx:26-58`; `app/accessibility/page.tsx:94-113`; `app/about/page.tsx:130-137`; `.github/workflows/ci.yml:35-42`.
   - Decision: duplicate of existing ledger row; recurrence incremented and evidence widened.
3. Medium: `YENTL-UI-0001` recurs. Older document-shell routes still differ from the refreshed public launch shell and weaker return navigation remains visible in code. `/about` and `/methodology` match the same root issue, so they were folded into the existing row instead of added separately.
   - Evidence: `app/about/page.tsx:11-153`; `app/methodology/page.tsx:11-168`; `app/privacy/page.tsx:14-278`; `app/terms/page.tsx:13-149`; `app/subprocessors/page.tsx:14-106`; `app/accessibility/page.tsx:14-136`; `app/contact/page.tsx:11-63`.
   - Decision: duplicate of existing ledger row; recurrence incremented and route scope widened.

## Mobile findings

- No fresh mobile screenshot proof was captured because Browser verification was blocked. Prior Hadassah/Shira evidence remains the current mobile visual proof for `/session` and ingest states.
- Code still preserves mobile-safe session structure: `components/session/session-shell.tsx:143-161` uses compact wrapping tabs, and `components/session/session-shell.tsx:220-276` gives header actions 44px mobile heights.
- Source selection remains mobile-honest: mobile platforms remove current-tab capture and present shared link/file/text/mic paths with explicit copy that mobile browsers cannot expose another tab's audio like desktop Chrome.
  - Evidence: `components/session/source-picker.tsx:65-81`; `components/session/source-picker.tsx:197-240`; `components/session/source-picker.tsx:304-416`.
- Mobile risk is still concentrated in the older public document routes. The risk is continuity and return-path quality, not proven overflow from this run.

## Platform-truth findings

- Browser-tab capture is still framed as a desktop Chrome extension path in the source picker and browser-tab pane.
  - Evidence: `components/session/source-picker.tsx:197-210`; `components/session/ingest-panes/browser-tab-ingest-pane.tsx:77-185`.
- FAQ and pricing copy remain aligned with guest-first, browser-local saves and do not overpromise account sync or paid plans.
  - Evidence: `app/faq/page.tsx:16-29`; `app/pricing/page.tsx:36-44`; `app/pricing/page.tsx:115-124`.
- `/sessions` empty and saved states clearly label browser-local saves with no cross-browser/account sync in v1.
  - Evidence: `app/sessions/page.tsx:339-370`; `app/sessions/page.tsx:388-420`.
- `/project/flows` is labeled as internal planning, not an end-user route, and prior Miriam evidence still shows many target states intentionally missing.
  - Evidence: `app/project/flows/page.tsx:20-22`; `agent-work/miriam-flow-atlas/coverage-audit.md`.

## Public-copy risks

- Launch-critical: unsupported GPC support claim remains on `/privacy`.
- High-risk: accessibility conformance/audit certainty remains overstated on `/accessibility` and `/about`.
- Medium-risk: public informational routes still visually/read structurally like an older product generation, which weakens mobile continuity and launch polish.

## Ranked fixes

1. `YENTL-TRUTH-0001` - ready for fix
   - Narrow or remove the GPC support claim in `app/privacy/page.tsx:230-244`.
   - Verification: repo search should show no unsupported GPC public claim unless an implementation exists.
2. `YENTL-TRUTH-0002` - ready for fix
   - Narrow accessibility copy in `app/accessibility/page.tsx:26-58`, `app/accessibility/page.tsx:94-113`, and `app/about/page.tsx:130-137` to audited scope and conditional CI reality.
   - Verification: copy should distinguish target level, implemented features, conditional automated checks, and unverified manual/screenshot scope.
3. `YENTL-UI-0001` - keep as backlog/new, not ready_for_fix
   - Decide whether to introduce a shared public information/trust wrapper before route-by-route cleanup.
   - Verification: public route family review across `/about`, `/methodology`, `/privacy`, `/terms`, `/subprocessors`, `/accessibility`, and `/contact`.

## Ledger updates made

- No new rows were added.
- `YENTL-TRUTH-0001`: kept `ready_for_fix`; `Recurrences` changed from 1 to 2.
- `YENTL-TRUTH-0002`: kept `ready_for_fix`; `Recurrences` changed from 1 to 2; route/evidence widened to include `/about` and conditional CI evidence.
- `YENTL-UI-0001`: kept `new`; `Recurrences` changed from 1 to 2; route/evidence widened to include `/about` and `/methodology`.
- No `fixed_pending_verify` rows were present, so no rows were marked `verified_fixed` or `reopened`.
- Launch-critical alert remains active and `alerts.md` was updated.

## Recommended next action

Fix one narrow issue first: `YENTL-TRUTH-0001` on `/privacy`, then rerun this audit.
